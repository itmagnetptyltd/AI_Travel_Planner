/** Shown with every generated Plan (REQ-TRV-031); the emailed Plan reuses these words (REQ-TRV-032). */
export const PLAN_RECOMMENDATION_NOTICE =
  'These Activities, times and costs are recommendations, not guaranteed availability, prices or bookings.';
