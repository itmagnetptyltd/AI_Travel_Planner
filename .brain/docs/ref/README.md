# ref/

Screenshots, emails, and other files from chat attachments or `/note`.

Named `NNN-original.ext` (serial, then the file name). The dashboard Others
tab lists serial, filename, type, and an icon.

Files dropped in `../inbox/` are moved here on `/dashboard` or `/note`.

Do not put secrets here.
